# 技术架构文档

## 1. 架构概览

<!-- ASCII 或 Mermaid 架构图，展示系统的整体结构 -->

```
┌─────────────┐     ┌─────────────┐
│   模块 A     │────→│   模块 B     │
└─────────────┘     └─────────────┘
```

## 2. 模块划分

### 2.1 [模块名]

- **职责**: ...
- **对外接口**: ...
- **内部核心组件**: ...
- **依赖的其他模块**: ...

<!-- 为每个模块重复以上格式 -->

## 3. 模块间通信

<!-- 模块之间如何交互：API 调用、事件、消息队列、IPC 等 -->

### 接口定义

<!-- 如果项目有跨模块接口（如前后端 IPC），在此定义接口格式模板 -->

```
### [接口类型]: [接口名]
- 方向: [调用方] → [被调方]
- 参数: { field: type, ... }
- 返回值: { field: type, ... }
- 错误: 可能的错误类型
```

## 4. 数据流

<!-- 核心业务场景的数据流转图 -->

### 场景: [场景名]

```
用户操作 → 模块 A → 模块 B → 数据存储 → 返回结果
```

## 5. 目录结构

```
project/
├── ...
```

## 6. 核心接口签名

<!-- 关键模块的函数/类/API 签名，帮助开发 Agent 理解接口边界 -->
